# Backup pre-Bloque 12c + smoke real prod — 5 may 2026

Backup full prod tomado antes del refactor `lv_piv_zona*` → `lv_piv_ruta*`
(import 5 rutas oficiales + 81 municipios maestros del Excel
WINFIN_Rutas_PIV_Madrid.xlsx).

## Backup metadata

| Item | Valor |
|---|---|
| Fecha | 2026-05-05 08:23 (Madrid) |
| Servidor | SiteGround GoGeek MySQL 8.4.6 |
| Cliente dump | `mysql-client@8.4` |
| Tablas dumpeadas | 28 (14 legacy + 13 `lv_*` + `migrations`) |
| Tamaño plano | 12 MB |
| Tamaño cifrado | 1.0 MB |
| Cifrado | 7z AES-256 + header encryption + max compression |
| Passphrase | 32 chars alfanum, en password manager |

## Hashes

```
sha256 plano:    80c1e378f0856bb8fc257aa2e478bef191184a6e4f9a401e9cfbe77265ff1cc0
sha256 cifrado:  3eae23ac931b432069b3890a531928c323cd6cdd78e157d11ef36f7ba64681e1
```

## Copias

| Ubicación | Estado |
|---|---|
| `~/Documents/winfin-piv-backups-locales/prod-pre-bloque-12c-20260505-082326.sql` | local Mac |
| `~/Documents/winfin-piv-backups-locales/prod-pre-bloque-12c-20260505-082326.7z` | local Mac |
| `~/Documents/LENOVO1/WINFIN PIVS/Backup Paneles Pivs/prod-pre-bloque-12c-20260505-082326.7z` | local Mac (segunda) |
| USB físico | Pendiente acción usuario |

## Smoke real ejecutado

**Pre-smoke baseline** (5 may 08:23):
- piv 575, lv_piv_archived 91, lv_revision_pendiente mayo 484 todas pendientes.
- lv_piv_zona 6 + lv_piv_zona_municipio 51 (datos del Bloque 12b.1 listos para drop).
- lv_piv_ruta NO existe.

**Migrate aplicado** (5 may ~08:25):
- `2026_05_05_000000_drop_lv_piv_zona_tables` — 54.67ms.
- `2026_05_05_000001_create_lv_piv_ruta_tables` — 179.47ms.
- SQL revisado vía `migrate --pretend` antes del `--force`: DROP × 2 + CREATE × 2 + UNIQUE compuesto + FK física `ruta_id` ON DELETE CASCADE + 2 indexes. **Cero ALTER/DROP legacy** ✓.

**Seeder aplicado**:
```
php artisan db:seed --class=PivRutaSeeder --force
Rutas: 5 · Municipios asignados: 40 · Skipped (sin match): 41
```

**Tinker assert post-seed**:
- `PivRuta::count() = 5` ✓
- `PivRutaMunicipio::count() = 40` ✓
- Distribución por ruta: ROSA-NO 16, ROSA-E 5, VERDE 10, AZUL 5, AMARILLO 4. Total = 40 ✓
- ROSA-E sample: Alcalá de Henares, Campo Real, Loeches, Pezuela de las Torres, Torres de la Alameda. Acentos preservados via Eloquent + Latin1String cast.
- Cobertura paneles: 165 con ruta + 319 sin ruta = 484 (319 vs ~316 esperado — diff +3 por paneles legítimos nuevos 560-575).

**Smoke UI Safari del usuario** ("OK smoke 12c"):
- Sidebar Operaciones → Rutas: 5 rutas con ColorColumn Carbon-aligned.
- Click ROSA-E slideOver: RelationManager Municipios con sus 5 municipios + km_desde_ciempozuelos.
- LvRevisionPendienteResource filter Ruta = "Rosa Este" → muestra paneles cuyo municipio está en ROSA-E.
- Filter Ruta = "Sin ruta" → muestra 319 paneles urbanos.

**Estado prod post-smoke verificado**:

| Métrica | Pre | Post |
|---|---|---|
| piv | 575 | 575 |
| lv_piv_archived | 91 | 91 |
| lv_piv_zona | 6 | **dropped** |
| lv_piv_zona_municipio | 51 | **dropped** |
| lv_piv_ruta | 0 (no existía) | **5** |
| lv_piv_ruta_municipio | 0 (no existía) | **40** |
| lv_revision_pendiente mayo pendientes | 484 | 484 |
| avería notas "Revisión preventiva mensual" | 0 | 0 |
| asignación tipo=2 fecha=today | 0 | 0 |

**Sin cleanup**: las 5 rutas + 40 asignaciones quedan permanentes en prod (uso real para 12b.4 filter Ruta + futuro planificador 12e).

## Lección aplicada (vs Bloque 12b.3)

Copilot **NO ejecutó** `php artisan migrate` ni tinker contra prod durante la
implementación del 12c. Cero contaminación factory esta vez. La migration y
seeder se aplicaron via Claude Code en sesión dedicada con backup previo +
`--pretend` revisión + OK explícito del usuario antes de `--force`.

## Próximos sub-bloques módulo 12

- **12d**: Importación CSV SGIP/ICCA (averías) con ADD + mark inactive + audit
  trail (`fecha_import`, `archivo_origen`, `imported_by_user_id`).
- **12e**: Planificador diario que cruza `lv_averia_icca` (12d) + `lv_revision_pendiente requiere_visita today + carry overs` (12b.3) + ruta optimizada por `km_desde_ciempozuelos`.
- **12f-i**: Asignación técnico, PWA ruta del día mixta, cierre admin + derivaciones (Clear Channel/industrial), reporte mensual contractual.
