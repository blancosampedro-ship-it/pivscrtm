# Backup pre-Bloque 12b.1 — 2026-05-04

Backup full de BD producción SiteGround tomado antes del smoke 12b.1 (CREATE TABLE
`lv_piv_zona` + `lv_piv_zona_municipio` + seed 6 zonas Madrid).

## Metadata

| Item | Valor |
|---|---|
| Fecha | 2026-05-04 09:53 (Madrid) |
| Servidor origen | SiteGround GoGeek MySQL 8.4.6 |
| BD | `dbpsdjqyvbnpch` (alias prod) |
| Cliente dump | `mysql-client@8.4` (Homebrew, paralelo a 9.3 default) |
| Flags `mysqldump` | `--single-transaction --quick --no-tablespaces --skip-lock-tables --routines --triggers` |
| Tablas dumpeadas | 25 (14 legacy + 10 `lv_*` + `migrations`) |
| Tamaño plano | 12 MB |
| Tamaño cifrado | 1.0 MB (ratio 12×) |
| Cifrado | 7z AES-256 con header encryption (`-mhe=on -mx=9 -ms=on`) |
| Passphrase | 32 chars alfanum, ~190 bits entropía. **NO en este archivo** — password manager + segundo holder. |

## Hashes

```
sha256 plano:    8a1742953eaa0bfe112f1fbcdf2df442e5380aa435f983c9ae826f1312ebf7ca
sha256 cifrado:  16c8251396ee53b68088a3fda5e7e697a6e0da1f9ef4c88088bc30bd6ee89b2f
```

## Copias

| Ubicación | Estado |
|---|---|
| `~/Documents/winfin-piv-backups-locales/prod-fullbackup-20260504-095256.sql` (plano) | local Mac |
| `~/Documents/winfin-piv-backups-locales/prod-fullbackup-20260504-095256.7z` (cifrado) | local Mac |
| `~/Documents/LENOVO1/WINFIN PIVS/Backup Paneles Pivs/prod-fullbackup-20260504-095256.7z` | local Mac (segunda copia) |
| USB físico | **Pendiente acción usuario** — arrastrar `.7z` a USB para copia off-Mac real |

## Verificación cifrado

Hecho post-cifrado: descifrado a temp dir con la passphrase → sha256 del descifrado coincide con
sha256 plano original (`8a17…f7ca`) + 25 `CREATE TABLE` dentro. Archivo recuperable.

## Procedimiento de restauración

```bash
# 1. Descifrar
PASS="<passphrase del password manager>"
7z x -p"$PASS" prod-fullbackup-20260504-095256.7z

# 2. Verificar integridad
shasum -a 256 prod-fullbackup-20260504-095256.sql
# Debe coincidir con: 8a1742953eaa0bfe112f1fbcdf2df442e5380aa435f983c9ae826f1312ebf7ca

# 3. Restaurar (a BD nueva o existente)
/usr/local/opt/mysql-client@8.4/bin/mysql \
  -h "$DB_HOST" -P "$DB_PORT" -u "$DB_USERNAME" -p"$DB_PASSWORD" \
  "$DB_TARGET" < prod-fullbackup-20260504-095256.sql
```

## Estado de la BD en el momento del backup

- `piv` legacy: 575 filas (intacto desde Bloque 01).
- `lv_users`: 2 filas (id=1 admin `info@winfin.es`, id=2 técnico smoke 66 status=0).
- `lv_piv_archived`: 91 filas (bus rows del Bloque 07e).
- `lv_correctivo_imagen`: 0 filas (smoke 11b limpiado).
- `correctivo`: +1 fila id=65902 (smoke Bloque 09 con asignación 32439).

## Próximos pasos antes del smoke 12b.1

Con backup verificado, listos para:
1. `php artisan migrate --pretend` (revisar SQL: 2 CREATE TABLE `lv_piv_zona*`).
2. `php artisan migrate --force` real.
3. `php artisan db:seed --class=PivZonaSeeder --force`.
4. Verificación tinker + UI admin + screenshots.

(Detallado en `status.md` próximo paso natural).
